<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelompok.jadwalku">

    <!-- ═══════════════════ PERMISSIONS ═══════════════════ -->

    <!-- Internet untuk Firebase / Firestore -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Agar AlarmManager bisa bangunkan device saat sleep -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Alarm tepat waktu (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Fallback untuk Android 13+ jika SCHEDULE_EXACT_ALARM tidak granted -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Notifikasi (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Re-schedule alarm setelah reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".PengingatApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jadwalku">

        <!-- ═══════════════════ ACTIVITIES ═══════════════════ -->

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"         android:exported="false" />
        <activity android:name=".TambahJadwalActivity" android:exported="false" />
        <activity android:name=".TambahTugasActivity"  android:exported="false" />
        <activity android:name=".PengingatActivity"    android:exported="false" />
        <activity android:name=".NotifikasiActivity"   android:exported="false" />

        <!-- ═══════════════════ RECEIVERS ═══════════════════ -->

        <!--
            AlarmReceiver: menerima trigger dari AlarmManager dan menampilkan
            notifikasi lokal. Bekerja 100% offline setelah alarm dijadwalkan.
        -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />

        <!--
            BootReceiver: menjadwalkan ulang semua alarm setelah device reboot,
            karena AlarmManager kehilangan semua alarm saat device dimatikan.
        -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>